# 공통
APP_ENV=dev
LOG_LEVEL=INFO

# FastAPI
API_HOST=0.0.0.0
API_PORT=8100

# LLM
VLLM_BASE_URL=http://127.0.0.1:8101/v1
VLLM_MODEL_NAME=Qwen/Qwen2-7B-Instruct

# Observability DB (Postgres + TimescaleDB)
POSTGRES_USER=pretrend
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=pretrend_obs
POSTGRES_PORT=1234
POSTGRES_HOST=localhost

# Legacy DB variables (기존 참조 경로 호환용)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=pretrend_user
DB_PASSWORD=CHANGE_ME
DB_NAME=pretrend

# 기타 키/토큰 (실제 값은 .env에서만)
OPENAI_API_KEY=DEMO_KEY
NEWS_API_KEY=DEMO_KEY
